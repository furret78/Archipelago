folder guide

client = options and client
    options.py (dataclass)
    options_classes.py (options)
    client.py (I don't need to explain this)
    game_controller.py (handles memory editing)
    webworld.py (something for web)
icons = client icon
tests = misc stuff that won't be included in the apworld
docs = docs but same as above
utils = various utilities used everywhere
variables = constants used everywhere
worldgen = world generation stuff
    world_items = items.py and item table
    world_locations = locations.py and location table
    world_rules = rules.py and related rules utils
    regions.py